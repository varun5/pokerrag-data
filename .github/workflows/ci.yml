name: Data CI
on:
  push: { branches: [ main ] }
  pull_request: { branches: [ main ] }
jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "pokerrag-data scaffold CI"
